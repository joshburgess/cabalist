//! cabalist-tui library — exposes internal modules for integration testing.

/// Application state, event loop, and core logic.
pub mod app;
/// Color palette and style definitions.
pub mod theme;
/// Full-screen rendering modes (dashboard, deps, extensions, etc.).
pub mod views;
/// Reusable UI components (header, search, status bar).
pub mod widgets;
